type: object
properties:
  code:
    type: number
    enum: [4]
  status:
    type: string
    enum: [DEADLINE_EXCEEDED]
  message:
    type: string
  details:
    $ref: google_cloud_api.error_details.yaml
