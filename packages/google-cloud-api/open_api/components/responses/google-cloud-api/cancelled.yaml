description: 499 Client Closed Request
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          $ref: ../../schemas/google-cloud-api/error=cancelled.yaml
