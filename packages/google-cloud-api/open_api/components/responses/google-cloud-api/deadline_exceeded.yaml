description: 504 Gateway Timeout
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          $ref: ../../schemas/google-cloud-api/error=deadline_exceeded.yaml
