description: 400 Bad Request
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          $ref: ../schemas/google-cloud-api.error.error-invalid_argument.yaml
    examples:
      not_empty:
        $ref: ../examples/google-cloud-api.error-invalid_argument.yaml
